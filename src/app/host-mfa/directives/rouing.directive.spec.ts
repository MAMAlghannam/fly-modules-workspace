import { MFARouingDirective } from './rouing.directive';

describe('RouingDirective', () => {
  it('should create an instance', () => {
    const directive = new MFARouingDirective();
    expect(directive).toBeTruthy();
  });
});
